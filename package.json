{
  "name": "born_to_review",
  "version": "0.0.1",
  "description": "A visulization project to render the pull requests review relations in a project",
  "main": "server/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node ./server/server.js",
    "build-client": "browserify ./client/app.js -o ./public/js/bundle_app.js"
  },
  "author": "_ (:3_| L) _",
  "license": "MIT",
  "dependencies": {
    "@octokit/rest": "^16.35.0",
    "axios": "^0.19.0",
    "browserify": "^16.5.0",
    "d3": "^5.13.1",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "tracer": "^1.0.1"
  }
}
